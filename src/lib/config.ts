/**
 * Configuration for the Lattice MCP server.
 */

export const LATTICE_URL =
  process.env.LATTICE_URL || "https://lattice.atomic.health";

export const LATTICE_COLLECTION =
  process.env.LATTICE_COLLECTION || "";
